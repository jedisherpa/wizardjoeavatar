{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core window capability; lifecycle operations are limited to typed Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
