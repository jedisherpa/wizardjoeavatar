# Coding-agent prompt — JV-CAS-EXTERIOR-GOLD-LEGACY

Implement the Joeville scene `JV-CAS-EXTERIOR-GOLD-LEGACY` from this branch without changing character identity assets.

Read `SCENE_CONVERSION.md` and `SCENE_CONTRACT.json` first. Treat their status and corrections as blocking gates. If the concept requires recomposition or redesign, do not silently integrate it as-is; produce the corrected clean master, inspect it, update provenance and then proceed.

Use the shared environment runtime foundation. Preserve the source file byte-for-byte. Add deterministic scene registration, layer loading, normalized anchors, foreground occlusion, resize behavior, fallbacks, focused tests and runtime evidence. Keep characters separate from scenery. Do not claim video, parallax, weather or full-cast validation unless each exists and has been inspected.

The work is done only when the branch acceptance gate passes and the live visualizer can select `JV-CAS-EXTERIOR-GOLD-LEGACY` explicitly while existing character-only scenes continue to pass.
