# Agent Handoff

**Goal.** Produce evidence-first repository-specific documentation suite for `jedisherpa/wizardjoeavatar` using only supplied frozen repository report, final lead-orchestrator synthesis, shared verified evidence, and exact production baseline.

**Canonical sources.** Frozen repository report sections 1–16, `discovery/production-sources.yml`, `discovery/relationship-graph.md`, `discovery/contradiction-log.md`, `discovery/authority-hierarchy.md`, `discovery/harvest/first-party-reference-edges.tsv`, `discovery/wizardjoe/relevant-branches.tsv`.

**Production branch.** `codex/rust-chatbot-animation-engine` at `e3c5c5d8fa39b16a2da6a48215a28dc84eee38e5`. The default `main` branch at `59f17bb2e534e987242b5c7ae2b47f42f95c8792` remains specification-oriented and is not the frozen implementation baseline.

**Repository state requirements.** Repository state must cite concrete evidence. Current implemented behavior cited only from concrete files, symbols, tests, workflows, manifests at frozen commits. Partial and planned behavior clearly labeled. No inference of integration from shared names. Preserve current working architecture unless synthesis justifies change through code evidence.

**Architecture summary.** Server-authoritative Rust animation engine with ASCILINE contract on analyzed branch. Orthogonal state regions. Content-free Prism signals. No governance bypass.

**Implementation boundaries.** No Telegram, persona, broadcast, learning, community, world, animation, agent, or convenience layer may bypass governance or identity boundaries. Planned work never described as complete.

**Required discovery.** Inspect anchored contents of omitted server source files. Verify rchat gate records. Confirm deployment receipt. Capture live browser every-frame evidence.

**Verification.** All claims Rank 1–4 evidence only. Exact-commit verification on every doc update.

**Human approvals.** Owner decision on C-001; canonical Telegram identity; live embedder health; unsigned build gating.

**Prohibitions.** Do not override canonical governance sources, CDISS, security policy, or human approvals. Do not invent field-level schemas. Mark unresolved interfaces as proposals requiring interface lock.

**Done conditions.** Exactly ten files returned. At least 13000 characters of substantive repository-specific content. All current-state claims cite concrete evidence. No commentary outside file blocks.
