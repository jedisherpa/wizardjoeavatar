# WizardJoe Python News-Anchor Implementation Packet

This directory carries the implementation-preparation packet for the Python ASCILINE workstream. It does not implement runtime behavior.

- Baseline before this packet: `62d7adee667c73e5f572eef73fb55a041bd5ee92` on `codex/python-asciline-avatar`.
- Packet: `WizardJoe_Python_News_Anchor_Implementation_Packet.zip`.
- SHA-256: recorded in `CHECKSUMS.sha256`.
- Scope: all 88 canonical news-anchor poses, 30 essential transitions, shared schemas/contracts, current Python/Rust/Joe architecture, Python file/symbol plan, dependency backlog, tests, governance, risks, rollback, visual references, and the master coding-agent prompt.

Begin inside the ZIP with `ENGINE_PACKET_README.md`, then read `MASTER_CODING_AGENT_PROMPT.md`. The Python branch remains the port-8765 development/runtime workstream. Generated content may only submit the semantic newsroom cue contract; raw pose and clip IDs are forbidden at the Joe boundary.

The clean baseline test run completed with 154 tests passing. Runtime news-anchor work remains `PLANNED_NOT_STARTED` in the packet.
