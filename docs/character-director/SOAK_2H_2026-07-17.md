# Two-Hour ASCILINE Soak Receipt

Date: 2026-07-17

## Command

The isolated Python candidate on port `8875` was exercised with the strict
source-soak harness. The existing service on port `8765` remained live and was
not used by the run.

```bash
.venv/bin/python tools/run_python_avatar_soak.py \
  --url http://127.0.0.1:8875 \
  --duration-seconds 7200 \
  --viewers 8 \
  --slow-viewer \
  --strict \
  --output evidence/character-director/soak-2h-2026-07-17.json
```

The connector credential was supplied through the environment and is not
present in the command receipt or evidence file.

## Result

The strict gate passed after 7,200.178 seconds.

| Measurement | Result |
| --- | ---: |
| Simulation cadence | 59.986 Hz |
| Final presentation window | 23.1 FPS |
| Viewers | 8 |
| Normal-viewer frames | 1,173,298 |
| Slow-viewer frames | 29,627 |
| Requests | 60,821 |
| Control commands | 54,254 |
| Prism signals | 2,345 |
| Request latency p50 | 24.197 ms |
| Request latency p95 | 55.538 ms |
| Maximum request latency | 5,370.707 ms |
| Command errors | 0 |
| Viewer errors | 0 |
| Decode errors | 0 |
| Sequence regressions | 0 |
| Hub queue drops | 0 |
| Schedule overruns | 4,779 |

Evidence:

```text
7f57f50ed191a5b182f00b569d3db0909e7a6011b3c615415afe21e1a3e37b82  evidence/character-director/soak-2h-2026-07-17.json
```

## Interpretation

The run proves two hours of stable fixed-step simulation, presentation,
concurrent viewer delivery, slow-client isolation, authenticated controls,
speech commands, and Prism advisory ingress without ordering, decode, command,
viewer, or queue-drop failures.

The 5.37-second maximum request latency and 4,779 schedule overruns are retained
rather than hidden. The p95 request latency remained 55.538 ms and the strict
gate passed, but these tails should be compared in the eight-hour and 24-hour
runs. This harness does not sample process RSS, so it does not by itself prove
the absence of memory growth.

## Remaining Gates

- Add bounded RSS sampling and growth thresholds.
- Complete the eight-hour and 24-hour runs.
- Exercise explicit connector loss/reconnect during a long run.
- Repeat against the immutable packaged application under an independent user
  or clean machine.
