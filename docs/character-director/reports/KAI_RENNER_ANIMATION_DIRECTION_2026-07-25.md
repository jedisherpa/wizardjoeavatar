# Kai Renner Animation Direction

Date: 2026-07-25
Status: 48-pose review candidate; product approval pending

## Acting Purpose

Kai's supplied 36 poses cover profile and front locomotion, running, jumping,
turning, grounded recovery, neutral holds, and a thumbs-up. The twelve authored
frames add grounded service exchange and craft explanation without repeating
those actions.

The `g7` `counter_service_exchange` performance follows:

`receive -> welcome -> listen -> confirm -> acknowledge -> reopen`

The `g8` `craft_quality_explanation` performance follows:

`collect -> frame subject -> first criterion -> compare -> precision -> approve`

Kai's acting remains compact, practical, and audience-aware. Low open palms
welcome or release the floor, small sternum-level gestures carry confirmation
and precision, and two-hand shapes frame or compare without becoming broad
presentational choreography.

## Motion Contract

- `g7` family: `counter_service_exchange`
- `g7` loop mode: `loop`
- `g8` family: `craft_quality_explanation`
- `g8` loop mode: `hold_last`
- Playback rate: 8 fps
- Root: fixed
- Support: dual-foot grounded
- Interrupt policy: marker-safe
- `g7` entry: front or three-quarter idle
- `g7` exit: front idle, three-quarter idle, or `g8`
- `g8` entry: front idle or `g7`
- `g8` exit: front idle or front walk

## Visual Identity Gate

Static contact-sheet inspection passes Kai's identity gate for review
projection. Across authored poses `037`-`048`, the black `BAKE` cap remains
legible and unmirrored; the square black glasses, brown beard and mustache,
yellow sweater, blue trousers, black shoes, voxel construction, and warm skin
palette remain recognizable. No prop, costume substitution, raised-heel
locomotion, or cropped authored silhouette is evident in the contact sheet.

This is a static artifact judgment, not browser or playback evidence. Product
visual approval remains pending, and the candidate remains review-only with
`runtime_admitted: false`.
