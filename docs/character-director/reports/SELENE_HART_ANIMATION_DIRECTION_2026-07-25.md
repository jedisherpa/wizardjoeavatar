# Selene Hart Animation Direction

Date: 2026-07-25
Status: 48-pose review candidate

## Acting Purpose

Selene's supplied set already covers side and front locomotion, running,
jumping, turning, landing, and hoop spectacle. The missing twelve poses extend
her usefulness in dialogue and governed interaction without repeating those
existing actions.

The first performance follows:

`receive -> raise -> frame -> indicate -> explain -> resolve`

The second follows:

`prepare -> establish -> boundary -> verify -> redirect -> handoff`

The hoop is an owned continuity object and a visible criterion, not loose
decoration. Gestures stay compact enough for speech performance. The boundary
is calm and low, while the redirect opens toward a constructive next step.

## Motion Contract

- `g7` family: `prop_communication`
- `g7` loop mode: `loop`
- `g8` family: `governance_boundary`
- `g8` loop mode: `hold_last`
- Root: fixed
- Support: dual-foot grounded
- Interrupt policy: marker-safe
- Entry and exit: front idle

## Visual Locks

- Warm brown skin tone remains stable.
- Long dark-brown textured curls retain their volume and side framing.
- Sage sleeveless top and deep teal fitted pants stay unchanged.
- Brown open-toe sandals remain planted on one baseline.
- The hoop retains its fixed diameter, thickness, and coral, ivory, and dark
  teal bands.

Product visual approval remains pending.
